[group:{{ groupName }}]
programs={{ programs . groupName }}

{{ range . }}
[program:{{ printf "%s-%s" groupName .Id }}]
directory = {{ .Directory }}
autorestart = true
autostart = true
user = {{ .User }}
command = {{ .Command }}
environment = {{ envs . groupName }}
{{ end }}