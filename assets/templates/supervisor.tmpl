[group:{{ groupName }}]
programs={{ programs . }}

{{ range . }}
[program:{{ .Id }}]
directory = {{ .Directory }}
autorestart = true
autostart = true
user = {{ .User }}
command = {{ .Command }}
environment = {{ envs . groupName }}
{{ end }}